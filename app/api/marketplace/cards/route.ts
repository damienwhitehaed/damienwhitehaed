import { prisma } from '@/lib/prisma';
import { NextResponse } from 'next/server';
export async function GET(req:Request){const {searchParams}=new URL(req.url); const where:any={}; if(searchParams.get('name')) where.name={contains:searchParams.get('name')!,mode:'insensitive'}; if(searchParams.get('grade_company')) where.gradeCompany=searchParams.get('grade_company'); if(searchParams.get('grade')) where.gradeValue=searchParams.get('grade'); if(searchParams.get('mode')) where.listingMode=searchParams.get('mode'); const min=searchParams.get('min_value'); const max=searchParams.get('max_value'); if(min||max) where.estimatedValueCents={gte:min?Number(min):undefined,lte:max?Number(max):undefined}; const cards=await prisma.card.findMany({where}); return NextResponse.json(cards);} 
